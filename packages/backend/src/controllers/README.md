# Controllers

This directory contains request handlers that process HTTP requests and return responses.

Controllers handle:

- Request validation
- Service layer coordination
- Response formatting
- Error handling

Controller files:

- `AccountController.ts`
- `FeatureController.ts`
- `AuthController.ts`
- `SessionController.ts`