# Configs

This directory contains configuration files for various application components.

Configuration files:

- `database.ts` - Oracle database connection settings
- `jwt.ts` - JWT configuration and secrets
- `environment.ts` - Environment variable management
- `server.ts` - Express server configuration
- `logging.ts` - Logging configuration
